# Telegram Bot для проверки банковских чеков

Telegram бот для проверки подлинности банковских чеков через API datagrab (https://api.datagrab.ru).

## Возможности

- ✅ Проверка PDF чеков от банков России
- 👥 Система контроля доступа (владелец добавляет пользователей)
- 🔒 Безопасное хранение токенов через переменные окружения
- 📊 Подробный отчет о проверке с данными транзакции

## Быстрый старт

### 1. Установка зависимостей

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Заполните следующие поля в файле `.env`:

- **BOT_TOKEN** — токен вашего Telegram бота (получите в @BotFather)
- **DATAGRAB_API_KEY** — ключ API datagrab (регистрация на https://bankpdf.ru)
- **DATAGRAB_ENDPOINT** — эндпоинт API (по умолчанию `https://api.datagrab.ru/upload.php`)
- **OWNER_ID** — ваш Telegram user ID (узнайте в @userinfobot)

⚠️ **ВАЖНО**: Если вы ранее публиковали токен бота, немедленно отзовите его в @BotFather командой `/revoke` и создайте новый!

### 3. Запуск бота

```bash
export $(cat .env | xargs)
python bot.py
```

Или используйте python-dotenv (уже в requirements.txt):

```python
# Добавьте в начало bot.py:
from dotenv import load_dotenv
load_dotenv()
```

## Использование

### Для владельца (OWNER_ID)

1. **Добавить пользователя:**
   ```
   /allow 123456789
   ```
   где `123456789` — Telegram ID пользователя (можно узнать через @userinfobot)

2. **Удалить пользователя:**
   ```
   /revoke 123456789
   ```

3. **Список разрешенных:**
   ```
   /list_allowed
   ```

### Для пользователей с доступом

1. Отправьте PDF файл чека боту
2. Дождитесь результата проверки

Бот поддерживает чеки следующих банков:
- Т-Банк (Tinkoff)
- Сбербанк
- Альфа-Банк
- ВТБ
- Райффайзенбанк
- И другие (полный список: https://api.datagrab.ru/get-banks/)

### Примеры результатов

✅ **Успешная проверка:**
```
✅ Чек Т-Банка

📋 Результат проверки:
Банк: tbank
Профиль: 1

💳 Данные чека:
Отправитель: Иван Иванович И.
Счет отправителя: ****4878
Получатель: Петр Петрович П.
Счет получателя: ****2323
Сумма: 1000 ₽
Статус: Успешно
Время: 05.12.2025 15:30:45
ID документа: 1-111-180-409-952
```

❌ **Поддельный чек:**
```
❌ Чек вероятно подделан
```

## Безопасность

- ❌ **НИКОГДА** не коммитьте файл `.env` в git (он уже в `.gitignore`)
- ❌ Не публикуйте токены в открытых чатах
- ✅ Используйте переменные окружения или менеджер секретов
- ✅ Регулярно ротируйте API ключи
- ✅ Ограничивайте доступ только проверенным пользователям

## Резервный сервер

Если основной сервер недоступен, измените в `.env`:

```bash
DATAGRAB_ENDPOINT=https://api2.datagrab.ru/upload.php
```

## Коды ошибок API

| Код | Значение |
|-----|----------|
| `forbidden` | Неверный API ключ |
| `unpaid` | Истек оплаченный период |
| `error` | Ошибка при обработке |
| `unrec` | Чек не распознан |
| `fake` | Чек вероятно подделан |
| `mod` | Чек создан через виртуальный принтер |
| `size` | Неверный размер файла |

## Структура проекта

```
bot_helper/
├── bot.py              # Основной код бота
├── requirements.txt    # Python зависимости
├── .env.example        # Пример конфигурации
├── allowed.json        # Список разрешенных пользователей
└── README.md          # Документация
```

## Поддержка

Если возникли проблемы:
1. Проверьте, что все переменные окружения заполнены
2. Убедитесь, что API ключ действителен
3. Проверьте логи в терминале
4. Убедитесь, что файл чека — настоящий PDF от банка

## Возможные улучшения

- 🔄 Webhook режим вместо polling (для production)
- 📧 Проверка чеков из email (mail_check функционал API)
- 💾 База данных для истории проверок
- 📊 Статистика использования
- 🎨 Inline кнопки для управления
- 🌐 Веб-интерфейс для администрирования
